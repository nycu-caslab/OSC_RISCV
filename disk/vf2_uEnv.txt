kernel_file = kernel.img
kernel_addr = 0x80200000
dtb_file    = jh7110-starfive-visionfive-2-v1.3b.dtb
dtb_addr    = 0x80600000

boot2=fatload ${bootdev} ${devnum}:${bootpart} ${kernel_addr} ${kernel_file}; fatload ${bootdev} ${devnum}:${bootpart} ${dtb_addr} ${dtb_file}; bootm ${kernel_addr} - ${dtb_addr}
